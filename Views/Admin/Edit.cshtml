@model VertoDevTest.Models.PageSection

@{
    ViewData["Title"] = "Edit Page Section";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="admin-container">
    <div class="admin-header">
        <h1>Edit Page Section</h1>
        <a asp-action="Index" class="btn btn-secondary">
            <span>← Back to List</span>
        </a>
    </div>

    <div class="form-container">
        <form asp-action="Edit" method="post" enctype="multipart/form-data" class="admin-form">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedDate" />
            <input type="hidden" asp-for="ImagePath" id="existingImagePath" />
            
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="SectionType" class="form-label"></label>
                    <select asp-for="SectionType" class="form-control">
                        <option value="">-- Select Section Type --</option>
                        <option value="Hero">Hero Slider</option>
                        <option value="Services">Services</option>
                        <option value="Portfolio">Portfolio</option>
                        <option value="About">About</option>
                    </select>
                    <span asp-validation-for="SectionType" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="DisplayOrder" class="form-label"></label>
                    <input asp-for="DisplayOrder" class="form-control" type="number" min="1" />
                    <span asp-validation-for="DisplayOrder" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Title" class="form-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Content" class="form-label"></label>
                <textarea asp-for="Content" class="form-control" rows="6"></textarea>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="form-label">Current Image</label>
                @if (!string.IsNullOrEmpty(Model.ImagePath))
                {
                    <div class="current-image">
                        <img src="@Model.ImagePath" alt="Current image" />
                    </div>
                }
                else
                {
                    <p class="text-muted">No image uploaded</p>
                }
            </div>

            <div class="form-group">
                <label for="ImageFile" class="form-label">Upload New Image (optional)</label>
                <div class="file-upload-wrapper">
                    <input type="file" name="ImageFile" id="ImageFile" class="file-input" accept="image/*" onchange="previewImage(event)" />
                    <label for="ImageFile" class="file-label">
                        <span class="file-icon">📁</span>
                        <span class="file-text">Choose a new image file</span>
                    </label>
                </div>
                <div id="imagePreview" class="image-preview"></div>
                <small class="form-text">Leave empty to keep current image</small>
            </div>

            <div class="form-group">
                <label asp-for="IsActive" class="checkbox-label">
                    <input asp-for="IsActive" type="checkbox" />
                    <span>Active (show on website)</span>
                </label>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">
                    <span>💾</span> Update Section
                </button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this section?')">
                    <span>🗑️</span> Delete
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function previewImage(event) {
            const file = event.target.files[0];
            const preview = document.getElementById('imagePreview');
            const fileText = document.querySelector('.file-text');
            
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.innerHTML = `<img src="${e.target.result}" alt="Preview" />`;
                    fileText.textContent = file.name;
                };
                reader.readAsDataURL(file);
            }
        }
    </script>
}

<style>
    .admin-container {
        padding: 2rem;
        max-width: 900px;
        margin: 0 auto;
    }

    .admin-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid #e0e0e0;
    }

    .admin-header h1 {
        font-size: 2rem;
        color: #2c3e50;
        margin: 0;
    }

    .form-container {
        background: white;
        padding: 2.5rem;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .admin-form {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .form-label {
        font-weight: 600;
        color: #2c3e50;
        font-size: 0.95rem;
    }

    .form-control {
        padding: 0.75rem;
        border: 2px solid #e0e0e0;
        border-radius: 4px;
        font-size: 1rem;
        transition: border-color 0.3s;
    }

    .form-control:focus {
        outline: none;
        border-color: #007bff;
    }

    textarea.form-control {
        resize: vertical;
        font-family: inherit;
    }

    .form-text {
        font-size: 0.85rem;
        color: #666;
    }

    .text-danger {
        color: #dc3545;
        font-size: 0.85rem;
    }

    .text-muted {
        color: #666;
        font-style: italic;
    }

    .alert {
        padding: 1rem;
        border-radius: 4px;
        margin-bottom: 1rem;
    }

    .alert-danger {
        background: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
    }

    .current-image {
        max-width: 400px;
    }

    .current-image img {
        width: 100%;
        border-radius: 4px;
        border: 2px solid #e0e0e0;
    }

    .file-upload-wrapper {
        position: relative;
    }

    .file-input {
        position: absolute;
        opacity: 0;
        width: 0.1px;
        height: 0.1px;
    }

    .file-label {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        border: 2px dashed #ddd;
        border-radius: 4px;
        cursor: pointer;
        transition: all 0.3s;
        background: #f8f9fa;
    }

    .file-label:hover {
        border-color: #007bff;
        background: #e7f3ff;
    }

    .file-icon {
        font-size: 2rem;
    }

    .file-text {
        color: #666;
    }

    .image-preview {
        margin-top: 1rem;
    }

    .image-preview img {
        max-width: 100%;
        max-height: 300px;
        border-radius: 4px;
        border: 2px solid #e0e0e0;
    }

    .checkbox-label {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        font-weight: normal;
    }

    .checkbox-label input[type="checkbox"] {
        width: 20px;
        height: 20px;
        cursor: pointer;
    }

    .form-actions {
        display: flex;
        gap: 1rem;
        margin-top: 1rem;
        padding-top: 1.5rem;
        border-top: 1px solid #e0e0e0;
    }

    .btn {
        padding: 0.85rem 1.8rem;
        border: none;
        border-radius: 4px;
        font-weight: 600;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        transition: all 0.3s;
        font-size: 1rem;
    }

    .btn-primary {
        background: #007bff;
        color: white;
    }

    .btn-primary:hover {
        background: #0056b3;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(0, 123, 255, 0.3);
    }

    .btn-secondary {
        background: #6c757d;
        color: white;
    }

    .btn-secondary:hover {
        background: #545b62;
    }

    .btn-danger {
        background: #dc3545;
        color: white;
        margin-left: auto;
    }

    .btn-danger:hover {
        background: #c82333;
    }

    @@media (max-width: 768px) {
        .form-row {
            grid-template-columns: 1fr;
        }

        .admin-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }

        .form-container {
            padding: 1.5rem;
        }

        .form-actions {
            flex-direction: column;
        }

        .btn-danger {
            margin-left: 0;
        }
    }
</style>